{
  "rules": {
    ".read": false,
    ".write": false,
    "live": {
      ".read": "auth != null",
      "$id": {
        ".write": "auth != null",
        ".validate": "newData.hasChild('t') && newData.child('t').isNumber()"
      }
    }
  }
}
